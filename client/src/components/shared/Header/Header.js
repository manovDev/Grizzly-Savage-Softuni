import './Header.scss';

function Header() {
    return (
        <header className="header">
            <div className="header-logo">
                Logo
            </div>
            
        </header>
    );
}

export default Header;
